Modules :

A module is just a file. One script is one module. As simple as that.

Modules can load each other and use special directives export and import to interchange functionality, call functions of one module from another one.

export :
export keyword labels variables and functions that should be accessible from outside the current module.

import :
import allows the import of functionality from other modules.



As our programm grows bigger , it may contain many lines of code. instead of putting everything in single file , you can use modules to seperate files according to their functions. This make our code organized and easier to maintain. 